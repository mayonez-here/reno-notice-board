# Copy this file to .env and fill in your own values.
# For TiDB Cloud (MySQL-compatible, free tier), the connection string looks like:
# mysql://<user>:<password>@<host>:4000/<database>?sslaccept=strict
DATABASE_URL="mysql://user:password@host:4000/notice_board?sslaccept=strict"
